{
  "scripts": {
    "install-bootstrap-glypicons": "cp -R node_modules/bootstrap-sass/assets/fonts/ _assets/fonts",
    "postinstall": "bundle install && chmod +x __tests__/test.sh && npm run install-bootstrap-glypicons && npm test",
  },
  "dependencies": {
    "jquery": "^3.1.0",
    "bootstrap-sass": "^3.3.7",
    "bootswatch": "^3.3.7"
  }
}
